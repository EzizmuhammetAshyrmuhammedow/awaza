<% if notice.present? %>
  <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
<% end %>

<div class="bg-base-200">
  <%= render 'index/landing_image' %>
</div>

<div class="my-5">
  <%= render 'index/book' %>
</div>

<%= image_tag asset_path('awaza-pan.jpg.gz'), class: "w-3/4 mx-auto mb-2" %>

<div class="w-full h-auto bg-base-200 px-20 pt-10">
  <%= render(HotelsComponent.new) %>
</div>
