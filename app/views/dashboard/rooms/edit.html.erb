<% content_for :title, t('rooms.edit_room') %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl dark:text-zinc-300"><%= t('rooms.edit_room') %></h1>

  <%= render "form", room: @room %>

  <%= link_to t('rooms.show_room'), @room, class: "ml-2 rounded-md px-3.5 py-2.5 bg-zinc-800 hover:bg-zinc-700 text-zinc-300 inline-block font-medium" %>
  <%= link_to t('rooms.back_to_rooms'), dashboard_rooms_path, class: "ml-2 rounded-md px-3.5 py-2.5 bg-zinc-800 hover:bg-zinc-700 text-zinc-300 inline-block font-medium" %>
</div>