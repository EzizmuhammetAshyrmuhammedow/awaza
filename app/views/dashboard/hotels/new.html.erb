<% content_for :title, "New hotel" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New hotel</h1>

  <%= render "form", hotel: @hotel, form_action: "new" %>

  <%= link_to t('hotel.back_to_hotels'), hotels_path, class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 dark:bg-zinc-800 dark:hover:bg-zinc-700 inline-block font-medium" %>
</div>
